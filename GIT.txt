git add "FCB.txt"

git commit -m "Meaningful message"

git remote add origin https://github.com/Ronixa584/TEMP.git

git branch -M main

git push -u origin main


Pull content i.e whemever other code is changed at remote git repo then first pull it and then push your content
git pull origin main